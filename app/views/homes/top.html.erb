<h1>Homes#top</h1>

<section>
    <h1>やることリスト</h1>
    <ul id="list">
    </ul>
</section>

<script>
'use strict';

let todo = ['デザインカンプ作成', 'データ整理', '勉強会申し込み', '牛乳買う', '歯医者へ行く'];
for(let item of todo) {
    const li = `<li>${item}</li>`;
    document.getElementById('list').insertAdjacentHTML('beforeend', li);
}
</script>
